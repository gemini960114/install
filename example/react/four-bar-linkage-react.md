幫我做一個曲柄搖桿機構的動畫,用 React 寫,Vite 建專案,跑 `npm run dev` 就能看。

畫面上要有一個滑桿,拖動的話可以控制曲柄轉動的角度,然後畫面即時畫出整組連桿的樣子,曲柄、連桿、搖桿都要看得出來是三根不同的線段,轉動的時候看起來要像真的機構在動一樣。

如果可以的話,搖桿末端(或連桿上隨便一個耦合點)移動的軌跡也順便畫出來,讓人可以看到那個點跑出來的曲線形狀。介面可以做得漂亮一點,畢竟是用 React,加個面板顯示目前角度之類的也可以。
