幫我用 React 做一個貪食蛇遊戲,用 Vite 建專案,跑 `npm run dev` 就能玩。整個專案建在 `D:\antigravity\snake-react`。

畫面上一條蛇,用方向鍵控制上下左右移動,吃到食物就變長一節,分數也加一分,螢幕上要看到目前分數。食物被吃掉後要在別的空位重新出現一個新的,不能吃完就沒了。撞到牆壁或咬到自己就算輸,跳出遊戲結束的畫面,然後可以按個按鈕重新開始。按跟目前前進方向相反的鍵不能讓蛇直接往回咬自己,而且按方向鍵的時候網頁本身不能跟著捲動。

顏色跟畫面隨便你設計,看起來順眼就好,速度不要太快,太快會玩不到。
